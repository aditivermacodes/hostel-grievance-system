# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=hgs_db
DB_USER=hgs_user
DB_PASSWORD=hgs_secret_2026

# Backend Application Configuration
SERVER_PORT=8080
SPRING_PROFILES_ACTIVE=prod

# JWT Authentication Secret (Must be 256-bit / 32 bytes or longer)
JWT_SECRET=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
JWT_EXPIRATION_HOURS=24

# File Storage Configuration
FILE_STORAGE_PATH=./uploads
MAX_FILE_SIZE_MB=5

# SMTP Email Delivery Configuration
# Set MAIL_SIMULATE=false to connect to a real SMTP server (Gmail, SES, SendGrid, etc.)
MAIL_SIMULATE=true
MAIL_FROM=notifications@hgs.internal
SPRING_MAIL_HOST=localhost
SPRING_MAIL_PORT=1025
SPRING_MAIL_USERNAME=
SPRING_MAIL_PASSWORD=
SPRING_MAIL_AUTH=false
SPRING_MAIL_STARTTLS=false

# Default Initial Seed Admin (Available automatically via Flyway migration V2)
ADMIN_INITIAL_USERNAME=admin
ADMIN_INITIAL_PASSWORD=Admin@Hgs2026!
